{
    "name": "graphql-deep-dedup",
    "description": "An Apollo Link library and test harness (GraphQL client and server) for GraphQL deduplication.",
    "version": "1.0.0",
    "scripts": {
        "lint-fix": "lerna run -- lint-fix",
        "lint": "lerna run -- lint",
        "test": "lerna run -- test",
        "clean": "lerna run -- clean",
        "build": "lerna run -- build",
        "prepush": "test -z \"$(git status --porcelain)\" || (echo \"Respository is dirty.\"; exit 1) && npm run lint && npm run test",
        "printDir": "npm run clean && tree -v --charset utf-8 -I node_modules",
        "push": "git push origin $(git rev-parse --abbrev-ref HEAD)"
    },
    "license": "UNLICENSED",
    "devDependencies": {
        "lerna": "^2.11.0"
    },
    "dependencies": {
        "@types/lodash": "^4.14.110"
    }
}
